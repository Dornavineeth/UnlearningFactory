# @package eval.tofu
# NOTE: the above line is not a comment, but sets the package for config. See https://hydra.cc/docs/upgrades/0.11_to_1.0/adding_a_package_directive/

defaults: # include all defined metrics files
  - tofu_metrics: 
    - Q_A_Prob
    # - Q_PARA_A_PARA_Prob
    # - Q_A_PERT_Prob
    # - Q_A_ROUGE
    # - Q_PARA_A_PARA_ROUGE
    - Q_A_PARA_ROUGE
    - Q_A_PERT_ROUGE
    - BIO_Prob
    # - BIO_ROUGE
    - Q_A_PARA_Prob
    - Truth_Ratio

handler: TOFUEvaluator
device: cuda
output_dir: ${output_dir} # set to default eval directory
metrics: {} # lists a mapping from each evaluated metric to its config 
# populated through the first (@package) line in each metric config
overwrite: false