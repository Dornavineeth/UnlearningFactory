
defaults:
  - .@forget: TOFU_QA_FORGET10  # Load the forget config by default
  - .@retain: TOFU_QA_RETAIN90  # Load the retain config by default

TOFU_QA_FORGET10_RETAIN90:
  group: unlearn
  args:
    forget_args: ${data.forget}
    retain_args: ${data.retain}